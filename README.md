# Go-Full-Crud

This is a remake from another project of mine, "Go-Crud" I was aiming for mocking a system with authentication workflow and a mock database... Now this should be only a CRUD application... I'm gonna use a real database and I'm not aiming for auth process... So every endpoint is public.
